<h1>Show page for a list <%= @list.name %></h1>
<%= @list.name %>
<% @list.bookmarks.each do |bookmark| %>
	<%= bookmark.comment %>
<% end %>

<%= image_tag(@list.image)  %>
<%= cl_image_tag (@list.image.key)%>
